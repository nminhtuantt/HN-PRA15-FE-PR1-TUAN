section
        nav.nv 
        - var menuItems = ['Trang chủ','/','Giới thiệu'];
        - var links = ['#', '#','#'];
        ul.menu
            each item, index in menuItems
                a.menu__link(href=links[index])= item