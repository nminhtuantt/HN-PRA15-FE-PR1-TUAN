section
        nav.nv 
        - var menuItems = ['Trang chủ','/', 'Blog'];
        - var links = ['#', '#','./blog.html'];
        ul.menu
            each item, index in menuItems
                a.menu__link(href=links[index])= item